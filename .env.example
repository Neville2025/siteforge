# Set these in your Vercel project (Settings → Environment Variables) or local .env.local

# Required for AI features (Magic Build, Magic Edit, AI Suggestions, AI Section Edit)
ANTHROPIC_API_KEY=sk-ant-...

# Optional. Only needed if you use the 21st.dev component search feature.
TWENTY_FIRST_KEY=
